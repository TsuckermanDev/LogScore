# Server Configuration
NODE_ENV=development
API_PORT=3001
WEB_PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=...
DB_NAME=logscore
DB_CONNECTION_LIMIT=10

# JWT Configuration
JWT_SECRET=...
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=*

# Logs Cleanup
CLEANUP_CRON=0 * * * *
# Cleanup every hour at minute 0

# Admin Default (created if not exists)
DEFAULT_ADMIN_LOGIN=admin
DEFAULT_ADMIN_PASSWORD=admin123
